$File = "log.txt";
    $Handle = fopen($File, 'a');
    $Data = "Runde " . $runde;
        fwrite($Handle, $Data);
    $Data = "guthaben " . $guthaben;
        fwrite($Handle, $Data);
    $Data = "gesetzt " . $gesetzt;
        fwrite($Handle, $Data);
    $Data = "gewinn_zahl " . $gewinn_zahl;
        fwrite($Handle, $Data);
    $Data = "gewinn_farbe " . $gewinn_farbe;
        fwrite($Handle, $Data);
    $Data = "gewinn_gerade " . $gewinn_gerade;
        fwrite($Handle, $Data);
    $Data = "verloren " . $verloren;
        fwrite($Handle, $Data);
    $Data = "gewonnen " . $gewonnen;
        fwrite($Handle, $Data);
    fclose($Handle);